<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>My Blog</title>
    <style>
        body { font-family: sans-serif; margin: 40px; background-color: #f9f9f9; }
        .container { max-width: 800px; margin: auto; background-color: white; padding: 20px; border-radius: 8px; box-shadow: 0 0 10px rgba(0,0,0,0.1); }
        h1 { color: #333; }
        a { color: #007bff; text-decoration: none; }
        .post-item { border-bottom: 1px solid #eee; padding-bottom: 15px; margin-bottom: 15px; }
        .post-item:last-child { border-bottom: none; }
        .post-item h2 { margin-top: 0; }
        .post-item a:hover { text-decoration: underline; }
        .post-item small { color: #6c757d; }
    </style>
</head>
<body>
    <div class="container">
        <h1>📖 My Blog</h1>
        <p><a href="/">&larr; 프로젝트 목록으로 돌아가기</a></p>
        <hr>
        <% posts.forEach(post => { %>
            <div class="post-item">
                <h2><a href="/blog/<%= post.id %>"><%= post.title %></a></h2>
                <small>작성일: <%= new Date(post.created_at).toLocaleDateString('ko-KR') %></small>
            </div>
        <% }) %>
    </div>
</body>
</html>