<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>블로그</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">블로그</h1>
        <div class="d-flex justify-content-end mb-3">
            <a href="/blog/write" class="btn btn-primary">새 글 작성</a>
        </div>

        <% if (posts.length > 0) { %>
            <ul class="list-group">
                <% posts.forEach(post => { %>
                    <li class="list-group-item">
                        <a href="/blog/<%= post.id %>" class="text-decoration-none">
                            <h5><%= post.title %></h5>
                        </a>
                        <small class="text-muted">작성일: <%= new Date(post.created_at).toLocaleDateString() %></small>
                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <p class="text-center">아직 작성된 글이 없습니다.</p>
        <% } %>
    </div>
</body>
</html>