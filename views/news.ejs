<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>실시간 뉴스 검색</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif; margin: 0; padding: 2em; background-color: #f4f4f9; }
        .container { max-width: 800px; margin: auto; background-color: #fff; padding: 2rem; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        h1 { color: #333; text-align: center; }
        .search-form { display: flex; margin-bottom: 2rem; }
        .search-form input[type="text"] { flex-grow: 1; padding: 0.8rem; border: 1px solid #ccc; border-radius: 4px 0 0 4px; font-size: 1rem; }
        .search-form button { padding: 0.8rem 1.5rem; border: none; background-color: #007bff; color: white; font-size: 1rem; cursor: pointer; border-radius: 0 4px 4px 0; }
        ul { list-style-type: none; padding: 0; }
        li { margin-bottom: 1.5em; padding: 1.5em; border: 1px solid #eee; border-radius: 8px; transition: box-shadow 0.2s; }
        li:hover { box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
        a { text-decoration: none; font-weight: bold; color: #0056b3; font-size: 1.2rem; }
        a:hover { text-decoration: underline; }
        p { color: #555; line-height: 1.6; margin-top: 0.5em; }
        .article-content { display: flex; }
        .article-image { margin-right: 1em; }
        .article-image img { max-width: 150px; height: auto; border-radius: 4px; }
        .article-text { flex-grow: 1; }
        .meta { font-size: 0.9em; color: #777; margin-top: 1em; }
        .no-results { text-align: center; color: #777; padding: 2rem; }
    </style>
</head>
<body>
    <div class="container">
        <h1>실시간 뉴스 검색</h1>

        <form action="/news" method="GET" class="search-form">
            <input type="text" name="query" placeholder="검색어를 입력하세요..." value="<%= query %>">
            <button type="submit">검색</button>
        </form>

        <ul>
            <% if (articles && articles.length > 0) { %>
                <% articles.forEach(article => { %>
                    <li>
                        <div class="article-content">
                            <% if (article.imageUrl) { %>
                                <div class="article-image"><img src="<%= article.imageUrl %>" alt="기사 썸네일"></div>
                            <% } %>
                            <div class="article-text">
                                <a href="<%= article.link %>" target="_blank" rel="noopener noreferrer"><%- article.title %></a>
                                <p><%- article.description %></p>
                            </div>
                        </div>
                         <div class="meta">
                            <span><%= new Date(article.pubDate).toLocaleString() %></span>
                        </div>
                    </li>
                <% }); %>
            <% } else { %>
                <li class="no-results">
                    <p>'<%= query %>'에 대한 검색 결과가 없습니다.</p>
                </li>
            <% } %>
        </ul>
    </div>
</body>
</html>